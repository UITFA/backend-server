export const destinationFolders = {
  comment: 'comment-files',
  lecturer: 'lecturer-files',
  point: 'point-files'
};

export const fileValidator = {
  fileType: /^(png|image\/png|jpeg|jpg|image\/jpeg)$/i,
  maxAvatarSize: 4 * 1024 * 1024,
  maxCoverSize: 6 * 1024 * 1024,
};
